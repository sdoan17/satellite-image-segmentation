# Optional. Leave blank to run the Vercel frontend as a curated-sample case study only.
# Set to the deployed FastAPI backend origin to enable live upload inference.
# Local example: http://localhost:8000
NEXT_PUBLIC_INFERENCE_API_URL=
